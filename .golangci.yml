version: "2"

run:
  go: "1.26"
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - bodyclose
    - errname
    - exhaustive
    - gocritic
    - misspell
    - nilerr
    - unconvert

  settings:
    errcheck:
      check-type-assertions: true
      check-blank: false
      exclude-functions:
        - fmt.Fprintf
        - fmt.Printf
        - fmt.Sprintf
        - fmt.Print
        - fmt.Println
        - fmt.Fprint
        - fmt.Fprintln
        - log.Printf
        - log.Println
        - log.Print
        - log.Fatal
        - log.Fatalf
        - log.Panic
        - log.Panicf

    gocritic:
      enabled-checks:
        - unlabelStmt
        - unnecessaryBlock
        - sloppyReassign
        - builtinShadow
      disabled-checks:
        - unlambda
        - elseif

    gosec:
      excludes:
        - G104

    revive:
      rules:
        - name: unused-parameter
        - name: unreachable-code
        - name: context-as-argument
        - name: error-return
        - name: var-naming
        - name: package-comments
          disabled: true
        - name: exported
          disabled: true

  exclusions:
    rules:
      - path: (example|exampleapp)/
        linters:
          - gosec
          - gochecknoglobals
          - revive
          - errcheck
          - noctx

      - path: _test\.go
        linters:
          - errcheck
          - gosec
          - gochecknoglobals
          - unparam
          - revive
          - unused
          - noctx
          - errname
          - staticcheck
          - prealloc
          - gofmt
          - goimports

      - path: _contract\.go
        linters:
          - errcheck

      - linters:
          - revive
        text: "unused-parameter"

      - linters:
          - errcheck
        source: "Dispatch\\["

      - linters:
          - gosec
        text: "G112"

      - path: nop\.go
        text: "error return value not checked"

      - linters:
          - errcheck
        text: "Error return value of `rows.Close`"

      - linters:
          - errcheck
        text: "Error return value of `(.*).Rollback`"

      - linters:
          - errcheck
        text: "Error return value of `db.Close`"

      - linters:
          - errcheck
        text: "Error return value of `(.*).Close`"

      - linters:
          - errcheck
        text: "Error return value of `job.RestoreJobState`"

      - linters:
          - errcheck
        source: "Addr\\(\\)\\.Interface"

      - linters:
          - errcheck
        source: "\\.FromJSON\\("

      - linters:
          - govet
        text: "inline: Constant reflect\\.Ptr should be inlined"

      - linters:
          - staticcheck
        text: "QF1"

formatters:
  enable:
    - gofmt
    - goimports

  settings:
    goimports:
      local-prefixes:
        - github.com/ddd-qce

issues:
  max-same-issues: 50
